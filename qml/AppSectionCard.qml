import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

// The card a group of related controls sits on.
Rectangle {
    Layout.fillWidth: true
    radius: Theme.radiusM
    color: Theme.cardBg
    border.width: 1
    border.color: Theme.divider
}
