# AI Reader 更新日志

## v1.3.24 — 2026-09-01

### 切换论文不再把同样的东西建两遍

- 一次点击，解读面板被构建了三遍。切论文时先把解读清空、再从课题里读回来，
  而这两半各自发了通知，于是面板把整棵树拆掉重建——先建成空的，再建成新
  内容——才安定下来。在外面看来这就是一次变化，现在也只通知一次。
- 把一篇论文的翻译放回去，原来是每段三次通知。一篇翻译完的论文是好几百段，
  而且每次打开都要来一遍。现在一次放回去。
- 「把新 PDF 交给阅读器」这一步——它在里面重新加载文档、重排页表——现在
  在日志里也有名字了，所以一次切换里剩下的时间无处可藏。

### 一次卡顿的明细，只属于这次卡顿
- 分阶段明细原来是在报告时才清空，而不是在事件开始时，于是两次慢事件之间
  发生的那些被标记的工作，会被算到后一次头上。各项相加大于总数——错误就是
  这样露出来的。现在每个事件都从零开始。

## v1.3.23 — 2026-09-01

### 最大化的窗口直接以最大化打开，中间不再闪一个小框
- 原来 QML 一建好窗口就把它显示出来，用的是 QML 里写死的尺寸，上次的窗口
  几何要晚一步才回来。这中间的那一步，就是你看到的先出小框再跳一下。
- 现在窗口在几何和最大化/全屏状态都恢复完之前一直不显示，然后直接以那个
  状态显示出来。第一帧就是最终形状。
- 窗口化时的尺寸依然单独保存，所以从最大化还原回去还是你上次拖的那个大小。

### 没打开过的面板不会被构建
- 九个面板原来在启动时全部构建，不管显不显示，而这些全都算在窗口出现之前
  的那几秒里。一个只开了四个面板的读者，照样在为文献夹、大纲、研究面板、
  任务列表和对话付账。
- 现在它们在第一次被打开时才构建，之后保留，所以再次开关不再有代价。收起
  五个面板时，窗口承载的元素从 1162 个降到 655 个。
- 拖动重排的手柄、保存的布局、各面板宽度，用的还是原来那套名字，排布面板
  的行为没有任何变化。

## v1.3.22 — 2026-09-01

### 统计解读的数量，不再需要把解读读一遍
- 现场日志里的明细没给任何猜测余地。每一次冻结都是同样两行，而且残差是
  零：2881 毫秒「从数据库里读某一类的全部对象」、1178 毫秒「解码课题里的
  全部解读」，然后 1899 + 772，再 955 + 394。QML 里没有，场景图里没有，
  Qt 里也没有。
- 一份存下来的解读，除了解读本身还带着它的前两个版本——几百 KB 的 JSON。
  十篇论文就是好几兆。而几乎所有关于解读的提问，想知道的都是「哪些论文有」
  「是谁的」「什么时候写的」，不是它写了什么：一共有多少份摘要（研究面板
  绑着这个，所以每次变化都要重问一遍）、这篇论文有没有别人写的、这一组
  的哈希是多少。每一个问题都要把全部内容解析一遍。
- 现在有了一张只装这些元数据的侧索引表，随对象到达同步维护——`paper_data`
  早就有一张一模一样的，出于同样的原因。统计、建索引、算哈希都只读它，
  一个载荷都不碰。
- 真正需要解读内容的地方，改走一个按各自 `updatedAt` 盖戳的解码缓存，
  所以一次只改动了一份解读的同步，只需要解码一份，而不是整个课题那么多。
- 用一个 200 篇论文、28 MB 解读内容的课题实测：一次变化从 416 毫秒降到
  13–23 毫秒，统计摘要数量从 47 毫秒降到 0。

## v1.3.21 — 2026-09-01

### 一次什么都没搬的同步，不再花掉一秒半
- 探针改成如实报告之后，现场日志点了名：每一次冻结都是「a sync landing」，
  每次 1.4 秒，一次接一次。而同步本身是**每两秒**来一次，不是每三十秒——
  其中包括这样一行：`sync: 0 objects in, 0 out, 1338 ms`。一次一个对象都
  没搬的同步，照样通知所有监听者重新加载，而每一个监听者都会重载文献库、
  重读课题里的解读、重跑绑定在它们上面的面板。
- 什么都没进来、什么都没出去，就没有任何东西需要反应，所以现在不再通知了。
- 本地每写一个对象，原来都会立刻要求一次完整同步。发布一篇论文的段落和
  翻译、存一份解读和它的笔记——每一件都是一次往返，而每一次往返最后都要
  干那件活。现在写入会等一秒半，和邻居一起走。
- 同步的接收方现在也会自报家门，所以剩下的那 1.4 秒有地方显形。

### 慢事件报告的是一份明细，不再是一个名字
- 探针原来只报事件里最长的那个标记步骤——只有在某一步占绝对大头时才是
  对的答案。现在它按耗时从大到小列出时间都去了哪里，最后一行是**完全没有
  任何标记覆盖**的那部分。那个数字才是「别再加标记了，去看 QML、场景图
  或者 Qt 本身」的信号。
- 计时是独占的：一条被标记的路径调用另一条被标记的路径，不会被算上里层的
  时间，所以各行相加而不是层层嵌套。

## v1.3.20 — 2026-09-01

### 每次跟服务器通信都要冻一下窗口的毛病没了
- 事件探针终于点了名：每一次冻结都是一次投递给 QNetworkReplyHttpImpl 的
  MetaCall——第一次 4075 毫秒，之后每次约 1400 毫秒。里面没有任何一个被
  标记的步骤，响应体全都小于半兆，其中一次同步只搬了一个对象。时间不在
  解析、不在写库、不在绘制、也不在排版，而在**发起请求**这一步上。
- Qt 每发一个请求都要问操作系统该用哪个代理，而且是在响应所属的那个线程
  上问——也就是正在画窗口的这个线程。Windows 上一旦开了「自动检测设置」，
  这个问题就是 WPAD：一次 DHCP/DNS 往返，某些网络下还要抓 PAC 脚本。问的
  人就在那儿卡着，直到有答案为止。
- 这个答案不可能变得那么频繁，不值得问第二遍。现在它在启动时问一次，在
  GUI 线程之外——比第一个请求早好几秒——之后整个会话都用缓存的答案。万一
  有请求跑在了查询前面，它会等一次，并且有上限；再往后都是瞬时的。
- 查询会把系统花了多久记进日志，所以一台机器上如果这就是全部问题，一行
  就能看出来。

### 卡顿日志不再误报
- 事件探针原来对所有线程的事件都计时，于是一个在后台老老实实建页的工作
  线程，看起来就像窗口冻住了。现在它只统计 GUI 线程。
- 它还是在事件结束**之后**才去读阶段名，那时候事件里的标记早就退栈了，
  答案永远是「idle」。现在它报告的是事件进行期间存活最久的那个标记，并且
  在里面确实没有任何标记时明说——正是这一点把矛头指向了 Qt 自己的代码。

## v1.3.19 — 2026-09-01

### 所有可能占住 GUI 线程的地方，现在都会自报家门
- 事件计时的门槛降到 150 毫秒，那一行也带上了更多东西：事件种类、接收
  对象的类名、它的 objectName、它父对象的类名，以及程序当时以为自己在做
  的事。
- 一次冻结现在会记下它持续期间线程投递了多少个事件。是 1 个，就说明是
  一个慢槽——而上面那一行正好点了它的名。是几千个，说明是大量小活堆出来
  的。**一个都没有**，说明线程根本不在投递事件，是在等锁、等线程或者等
  文件——那是完全另一种 bug，以前根本区分不出来。
- 另外二十条路径会在自己运行时报出名字：从数据库里读某一类的全部对象、
  解码课题里的解读、统计它们、重建和过滤论文列表、重载文献库、比较篮、
  把大纲和对话切到某篇论文并放回去、读写段落缓存和翻译缓存、给 PDF 算
  哈希、保存下载下来的 PDF、打包一份解读去存储、写设置文件。
- 一次同步会记下它搬了多少东西、花了多久；下载一个 PDF 会记下大小和耗时。
  成循环的活，在每一轮都低于冻结门槛时是完全看不见的；每轮一行，循环本身
  就藏不住了。

## v1.3.18 — 2026-09-01

### 日志现在能说出到底是什么冻住了窗口，不管有没有被标记过
- 手工标记只能覆盖有人想到要标的路径，而三轮现场日志回来都说卡顿不在
  那些路径里——与此同时逐帧计时同样确凿地排除了排版（`polish` 从没超过
  7 毫秒）和绘制（`blockedForSync` 从没超过 32 毫秒）。线程在忙一件谁也
  叫不出名字的事。
- 现在 GUI 线程投递的每一个事件都会计时。任何超过四分之一秒的，都会连同
  事件种类和接收对象的类名一起记下来——而排队的信号是以 MetaCall 的形式
  投递给「即将执行槽函数的那个对象」的，所以一个慢槽会自己报出自己的类名，
  不管有没有人标记过它。
- 代价是每个顶层事件两次时钟读取，而且只有事件真的慢了才会构造任何东西。
- 同步的推送一侧也标上了：把脏行读出来并序列化，是在这个线程上构建最多
  六兆的 JSON，每批一次——它是同步流程里最后一个没有名字的步骤。

## v1.3.17 — 2026-08-31

### 同步落地时不再冻住窗口
- 现场的逐帧计时把这件事定死了：`polish` 从没超过 1 毫秒，`blockedForSync`
  从没超过 32 毫秒——排版和绘制都不慢。GUI 线程只是整整 4.2 秒没碰
  Qt Quick，它在别的地方。而后面几次卡顿之间正好相隔 30 秒，那是同步的
  轮询周期。
- 一次拉取一页要两百个对象。对几百字节的文献条目这个数字是对的，对
  `paper_data` 就完全不对——那是一整篇论文的段落，或者一整篇论文的翻译。
  于是一页可能是几十兆的 JSON，在一次事件循环里解析完并写进数据库。
  现在一页是二十五个。
- 一页里的东西改成分片写入，每一轮大约一帧的量，中间把事件循环交还回去。
  同步总时长不变，但窗口全程可用。
- 解析响应是唯一没法分片的一步，所以它在进行时会在日志里报出自己的名字；
  超过半兆的响应还会连大小一起记下来。

### 新手指引的高亮框会跟着它指的东西走
- 指引把目标的位置经过每一层父节点映射出来，但一条绑定只依赖它读到的
  东西——目标自己的几何。父节点动了（分隔条归位、工具栏滚动、某个面板
  出现），目标在屏幕上就移了位，可这些都没被读到，于是高亮和卡片留在了
  旧布局的位置上。
- 启动慢的时候，这意味着整个指引都出不来：它在布局还没稳定时打开，之后
  再没有它依赖的东西发生变化，卡片就一直停在屏幕外，直到用户改了窗口
  大小才逼着绑定重算。现在它在打开期间每秒重新核对四次，代价可以忽略，
  也就不会再过期。

## v1.3.16 — 2026-08-31

### 卡顿现在会记下它是什么时候开始的，以及当时场景有多大
- 那一行现在同时带上冻结的起点和终点。在 t+22028 报出的「冻了 8374 毫秒」
  其实是从 t+13654 开始的；要把一次冻结和它里面的帧对上，两头都得有。
- 逐帧计时打开的时候，会顺带记下窗口尺寸和它当时承载的元素数量。给场景
  排版和把它交给渲染器，都是对每个元素走一遍，所以当问题是「一帧为什么
  要几秒」时，被走的那个东西有多大是最该先拿到的数字——也是唯一能把
  「机器慢」和「场景太大了」分开的数字。

## v1.3.15 — 2026-08-31

### 一次长时间的冻结，现在会自己打开逐帧计时
- 现场日志说窗口冻了四秒，而且当时那些昂贵的 C++ 路径一条都不在线程上
  ——所以时间花在了 QML 或者绘制里，可日志再也说不出是哪一个。
- 现在只要出现一秒以上的冻结，程序会自动把 Qt 自带的逐帧计时打开十五秒。
  之后每一帧都会记下 `polish`（这个线程在给场景排版）和 `blockedForSync`
  （它在等渲染器）。这两者指向完全相反的修法，而从外面根本无法区分。
- 在那之前它是关着的，因为每帧一行；十五秒后自己关掉。卡顿总是成串出现，
  所以在第一次就打开，后面几次照样抓得到。如果你自己设了 QT_LOGGING_RULES，
  程序不会去动它——那是你的配置。

## v1.3.14 — 2026-08-31

### 日志里的卡顿，现在会说清楚当时程序在干什么
- v1.3.13 能告诉你窗口冻了四秒，但说不出为什么：启动之后的一切都报成
  「idle」，而这恰恰是唯一没用的答案。现在阶段标记覆盖的是程序运行期间
  真正在做的事，不只是启动那一段。
- 那些可能占住 GUI 线程的路径，在自己跑着的时候会报出自己的名字：打开
  论文、加载 PDF 和它的段落、把这篇论文的翻译放回去、从课题里读解读、
  给课题的解读建索引、把这篇论文的成果打包共享、一次同步落地、以及消化
  同步带回来的东西。
- 如果一条卡顿仍然报「idle」，那本身就是信息而不是空白：说明时间花在了
  QML 或者绘制上，而不是上面这些活里的任何一件。

## v1.3.13 — 2026-08-31

### 日志现在会说清楚，卡住的那段时间花在哪了
- 「启动的时候卡住」这种反馈我们没法下手：日志只记了程序启动过，没记任何
  一段花了多久。现在启动的每个阶段都会打时间戳——读设置和钥匙串、恢复
  文献夹、构建同步和文献库服务、搭 QML 界面、重新打开上次开着的论文——
  统一对着 main() 开头启动的那个计时器。
- GUI 线程上还有一个看门狗，整个会话都开着。它是一个每秒该被叫醒二十次的
  定时器；它迟到多久，就说明线程被占了多久，窗口也就冻了多久。超过 300
  毫秒的都会写进日志，连同持续时长和当时程序正在做什么。启动之后的卡顿
  也一样覆盖——切换论文、一次同步落地——这些以前是完全没有痕迹的。
- 这两样常开的代价可以忽略，所以就常开着：碰上一次卡顿的人，证据已经在
  日志里了，不需要再用特殊版本去复现一遍。

## v1.3.12 — 2026-08-30

### 切换论文不再整秒地卡住窗口
- QtPdf 把所有对 PDFium 的调用都串在一把全局锁后面。原来每打开一篇论文，
  都会起一个后台任务，把整篇文档每一页的选区索引——文字、行框、链接框——
  全部建一遍，而这个任务一直占着那把锁。切到另一篇论文需要加载新文档、
  重排页面，用的是同一把锁，于是切换只能排在后面等。用真实论文量过：每次
  切换窗口冻住最多 1.1 秒，而且切得越快越糟，因为你已经离开的那篇论文的
  任务还在跑。现在是 80 毫秒。
- 那个全篇任务没有了。选区结构只给读者真正所在的那几页建——当前页和前后
  各两页——一次一页，跑在一个会把文档一直开着的线程上，不再为每一批重新
  解析一遍文件。只是翻过去的论文根本不会被解析；请求还会攒上零点几秒，
  所以快速滚动或者连着切标签，不会在身后留下一串没人看的页。
- 离开一篇论文，属于它的活就停下来。选区构建和段落聚类都会在每页之间问
  一句「还有人要这个答案吗」，所以一次切换最多等一页，而不是等一整篇。
- 点在索引还没建到的地方，行为和以前完全一样：那一页当场建出来。
- 论文的 id——文件前四兆的哈希——现在按文件、大小和修改时间记住。在两篇
  打开的论文之间来回切，原来每次都要重新从磁盘读四兆，去回答一个答案不
  可能变过的问题。

### 解读面板不再每次都把精读的九个部分全部重建
- 一次精读是九个部分，一开始只展开第一个，但九个都会被建出来；而且每一个
  都带着全部九种部分的排版，其实只用得上一种——整个面板放着 81 棵子树，
  真正需要的是 9 棵。模型每写完一个部分，这些全部丢掉重建一次。
- 现在一个部分是第一次展开时才建，每个部分只带自己的排版。面板里的元素
  数量降到原来的五分之一。显示一份解读从 360 毫秒降到 100 毫秒；每写完
  一个部分就发生一次的那次重建——一次精读要发生九次，每次同步还会再来
  一次——从 57 毫秒降到 11 毫秒。
- 第一次展开某个部分现在要花 60 毫秒左右，这正是要的效果：这份开销落在
  读者主动要看的地方，而且只花一次。

## v1.3.11 — 2026-08-30

### 解读的最大输出长度，可以调到和模型页一样高了
- 设置 → 解读 → 最大输出 token 数原来卡在 64000，而模型页上同名的那一格
  能到 131072。64000 是这个功能刚做出来那天随手定的整数，不是任何模型的
  上限；而且它偏偏是最不该被压低的一格——一次精读是九个独立请求，是整个
  程序里对模型要求最长的一件事。现在两格都到 131072，这个上限也收成了一个
  具名常量，不再是同一个数字分别写在输入框、写入和读取三个地方。
- 超过上限的值仍然会被挡回来而不是存进去；从另一台机器同步下来的设置也走
  同一道检查。

## v1.3.10 — 2026-08-29

### 窗口变窄时，工具栏不再跑到窗口外面去
- 工具栏是很长的一行。窗口比这一行还窄的时候，右半边——设置、帮助、账号、
  半个项目组——原来是画在窗口边界之外的，看不见，也点不到。现在它改成分
  三步让位。
- 先让位的是那些只用来看、点不动的读数：模型名先退场，账号只留图标去掉
  名字，项目下拉变窄，页码先丢掉文字、再整个退场。「LLM 未配置」不是读数，
  任何宽度下都留着——上面半排按钮点不动，原因就写在它身上。
- 还是放不下的部分改成横向滚动，两端各有一个箭头，告诉你那一侧还有东西；
  那一侧滚到头，箭头就暗下来。滚轮也能滚——普通的上下滚轮就行，多数鼠标
  只有这一个滚轮。
- 程序自己那一组永远不滚：设置、提示词、新手指引在任何窗口宽度下都钉在
  右边，始终一键可达。
- 什么时候该收起哪个读数，是量出来的，不是猜的。工具栏记着「全写开」和
  「去掉文字」两种状态下这一行各要多宽，再和实际可用的宽度比——所以同样
  一个窗口，没开论文时是宽裕的，开了论文、登了账号、选了项目就是紧的；
  而且收回某一步时比的是当初让它让位的那个数，这正是它不会在两个答案之间
  来回闪的原因。
- 新手指引会高亮工具栏上的按钮，而滚出去的按钮没法高亮，所以现在每一步都
  会先把自己要指的按钮滚回可见范围，再去找它。

## v1.3.9 — 2026-08-28

### 清空 API 密钥不会再把设置页面挤歪
- 密钥栏空着时下面那行红字，原来在模型页的两列网格里独占一格，于是它一
  出现，下面每一行都错开一格：温度、最大输出 token 数、上下文窗口全变成
  标签在右、控件在左。现在这行字放进密钥输入框自己那一格里，它显不显示，
  下面的行都不动。翻译页没有这行字，所以只有模型页会歪。

### 界面一次只说一种语言
- 每个输入框里右键弹出的菜单——撤销、重做、剪切、复制、粘贴、删除、全选
  ——是 Qt 自带的，而 Qt 没有为它准备中文，所以一整个中文界面里偏偏这个
  菜单是英文的。这七个词现在跟着程序一起翻译了。
- 程序自己只有中、英两种语言，Qt 却有几十种：德语或日语的 Windows 上，
  界面是英文的，对话框按钮和菜单却是德文或日文的——因为 Qt 有那些语言的
  词条而我们没有。现在语言先归到程序真正拥有的那两种之一，Qt 的文字跟着
  一起走：中文系统全是中文，其他系统全是英文。设置 → 界面 → 界面语言仍
  然可以手动指定。

## v1.3.8 — 2026-08-26

### 译文里的公式，现在是画出来的，不是拼出来的
- PDF 交给程序的公式本来就是被压扁的文本，翻译模型会忠实地把它们还原
  成 $…$ 的 LaTeX——段落界面原来把这些原样显示成一串美元符号。现在这
  些片段会渲染成真正的公式，嵌在译文句子里，墨色和字号都跟着正文走；
  暗色主题用自己的墨色，不再是深灰画在深底上看不见。$$…$$ 的独立公式
  单独一行居中。
- 渲染不了的片段保持原样文字，价格里的美元符号不会被误认成公式，没有
  公式的译文和从前显示得一模一样。

### 设置对话框会说密钥缺在哪
- API 密钥栏空着的时候，下面现在有一行红字说明：这样保存工具栏会一直
  显示「未配置LLM」；并且密钥只存在本机、不随账号同步——新机器上别的
  都同步好了、唯独差密钥，正是落在这里。

## v1.3.7 — 2026-08-26

### 每一种翻译入口，现在走同一套配置解析
- 启动翻译有四条路——整篇翻译、重试失败段落、右键翻译某一段、翻译选中
  文字——其中一条是特殊的：「重试」会沿用失败那一轮用过的端点和密钥，
  于是改好了设置再点重试，请求还是发往旧的、坏掉的端点。现在每个入口
  在发出请求之前，都按当前设置重新解析一遍。
- 修改模型设置现在立刻生效，哪怕还有段落在翻译中：新请求走新端点，已
  经在途的请求在旧端点上安静地跑完，不会被掐断。
- 设置对话框的翻译栏原来自己实现了一遍回落规则（「留空就沿用主配置」），
  所以「获取」按钮探测的，和翻译实际使用的可能不是同一个东西。现在两边
  走同一个解析函数，「翻译将使用」那一行也随输入实时预览——所见即所发。
- 密钥现在只跟着它自己的端点走：翻译栏指向的服务器只要不是主配置用的
  那一台，主配置的密钥就留在家里，不会被发给第三方。只差一个末尾斜杠
  的地址不再算「别处」，粘贴密钥时带进来的空白也不再算密钥的一部分。
- 模型返回的错误现在会写明是哪台服务器在说话——「HTTP 401(来自
  api.deepseek.com):…」——请求发错了地方一眼就能看出来，不会再被误读
  成正确端点上的密钥错误。

## v1.3.6 — 2026-08-26

### 缩放百分比那个按钮
- 工具栏上显示缩放百分比的地方，原来藏着两个手势：单击是按窗口宽度显示
  整页，双击是恢复 100%。为了分清这两下，每次单击都要先等上四分之一秒
  才有反应。旁边本来就有一个「按窗口宽度显示整页」的按钮在做前一件事，
  所以现在这个读数只做后一件：单击，立刻回到 100%，不再停顿。

## v1.3.5 — 2026-08-25

### 保存的布局那个菜单
- 列出已保存布局的菜单，对勾是画在布局名字上面的，于是正在用的那一套，名字
  和对勾叠印在一起，反倒成了唯一一套看不清名字的。现在这两样不再挤在同一个
  位置上。
- 每一行还带着一个小小的 ✕。在这个程序的其它地方，✕ 都是「关掉」的意思，
  在这里它却是把这套布局删掉。现在一行只做一件事——点一下就把那套摆法换回
  来——重命名和删除挪进了**「管理布局…」**窗口，那里的按钮用文字写明自己要
  做什么；删除会先问一句，就问在那一行上，并说清楚要删掉的是哪一套。
- 管理窗口里还会列出每套布局实际会打开哪些面板，于是一套布局可以按它到底做
  什么来分辨，而不是只能靠几个月前随手起的那个名字。

## v1.3.4 — 2026-08-25

### 面板布局现在可以保存和切换
- 面板想怎么摆就怎么摆，把这套摆法存成一个名字，之后在工具栏里随时切换。
- 一套布局记住的是哪些面板显示着、各自多宽、彼此的先后顺序——宽度存的是
  占窗口的比例，所以在大显示器上存的布局，搬到笔记本上依然合适。
- 布局跟着账号走，换一台电脑也是同一套摆法。窗口本身的大小和位置，特意
  没有算进布局里。

### 文库现在属于账号
- 退出登录会清空文库、搜索和解读这几个视图，不再把上一个人的论文留在屏
  幕上——什么都不会被删除，还没同步上去的东西仍然在，等那个人再登录回来
  照样会推送上去。
- 同一台电脑上的第二个账号，看到的是自己的文库，不是第一个人的。
- 只是暂时离线不算退出登录：文库属于谁，谁在没有网络的时候也照样能看到
  它。

### 登录后浏览器停留的页面，现在是张正经的卡片
- 应用自己的标志、一个绿色的对勾、用户的名字，还有一个关闭页面的按钮——
  浏览器不让脚本自己关页面时，会有一行字提示自己去关掉这个标签页。
- 失败的时候用的是同一张卡片，换成红色标记，并说明该怎么办。

### 修复
- 论文标题查不到的时候，窗口标题和标签页以前会显示一串 64 位的校验和——
  未登录时，或者课题里的论文还没同步下来时都会出现。现在改成显示「未命
  名论文」。
- 关着文库面板切换课题，重新打开文库面板时看到的还是上一个课题的论文列
  表；新建课题也可能让各个视图仍然停在旧课题上。
- 段落面板里选中的文字，以前是一整块不透明的深蓝色；现在跟 PDF 页面用
  的是同一种半透明高亮，同一句话在两边看起来是一样的。
- 段落里译文那一半，以前完全没法选中或复制，现在可以了。
- 任务列表里，停止任务的按钮以前挨着任务名字，看着像是要删掉这一行。现
  在挪到了进度条末尾，紧挨着它要停止的那项工作。

## v1.3.3 — 2026-08-25

### 面板按钮现在能看出面板是开还是关
- 工具栏上所有控制面板显示/隐藏的按钮，面板打开时呈按下状态，关闭时弹起——
  包括最新加的文库分析和任务列表这两个，之前它们根本没有按下状态。
- 面板被别的地方打开时按钮也不会说谎：选中文字问 AI 会打开对话面板，对话按钮
  现在知道这件事。

### 标签页的高亮块回到正中
- 当前标签背后的蓝色圆角块，距离外框上边 3 像素、下边 7 像素，看着说不上哪里
  不对但就是别扭。现在外框的高度由标签本身推算得出，上下间距相等。

## v1.3.2 — 2026-08-25

### 课题相关的按钮不再凭空消失
- 成员、课题画像、批量解读、对比、文库分析这几个按钮，以前在未登录或没选中
  课题时会直接从工具栏消失——这看起来像「功能被删了」，而不是「这个功能需要
  先做点别的」。现在它们保持在原位、变灰，鼠标悬停会说明缺的是什么。
- 点击它们不会毫无反应，而是直接去做缺的那一步：没登录就发起登录，没选课题
  就展开课题列表。

## v1.3.1 — 2026-08-25

### 文件夹面板真会把程序卡死，而且真的卡死过
- 浏览一个装满 PDF 的文件夹，以前会为屏幕上显示的每一行，当场遍历这个文件夹
  的整棵子目录树。文件夹大一点，或者是在网络驱动器上、在同步的云盘文件夹
  里，就会卡住整个窗口；而且因为这个面板会记住自己的状态，下次启动还会再
  卡一次。
- 更糟的是，这个遍历有时根本停不下来：文件夹里如果有个快捷方式指回它自己
  的某个上级目录（这在 Windows 用户目录和云盘挂载点里很常见），就会让遍历
  原地打转，永远转下去。
- 计数现在挪到后台进行：各行会立刻显示出来，数字随后再补上。遇到循环会被
  识别出来并跳过；论文数量多到离谱的文件夹，计数到一个上限就会如实报告，
  而不是给出一个「全选」，实际却只处理了其中一部分；勾选一个装满论文的文
  件夹，现在是瞬间完成，不用再等上几秒。

### 设置现在是一个你能读懂的文件
- 程序记住的一切，以前分别放在 Windows 的注册表、macOS 的偏好设置数据库，
  或者 Linux 的配置文件里——三种不同的东西，而且在 Windows 上根本不是个文
  件，所以程序打不开的人，没法被指导着去改哪里。
- 现在统一成一个 JSON 文件，在每个平台上都放在同一个位置，可以打开查看、
  备份、编辑、发给我们，或者直接删掉，让卡住的安装重新能用。它的位置会打
  印在启动日志里。
- 这个文件如果损坏了，程序会用默认设置启动，并把损坏的那份原样留在旁边，
  而不是直接覆盖掉。

### 你的设置现在能跟着账号走
- 属于你、而不属于某台机器的设置——模型和服务商、提示词、目标语言、界面语
  言、字号、Token 和并发上限、聊天里回车键的行为——现在跟着账号走，换一台
  电脑也能接着上次的用。
- 属于机器本身的设置留在本机：窗口大小和位置、哪些面板开着、多宽、每篇论
  文读到哪里、开着哪些文件、上次浏览到哪个文件夹。4K 显示器上的窗口位置，
  搬到笔记本上会跑到屏幕外，所以这些设置从不同步。
- API Key 和登录令牌从不离开本机——它们留在系统钥匙串里，不属于同步的内容。
- 已经在某台机器上配置过的东西，不会被账号里的设置覆盖：只有在这台机器上
  还没碰过的设置才会被采用，在这里改过的会被推送上去。不管有没有账号、有
  没有网络，程序始终按这台机器上的设置来。

## v1.3.0 — 2026-08-25

### 所有调用模型的操作，现在共用一个队列
- 翻译论文、切分段落、提取全文内容、把某一页当图片读取、快速解读和九个模块
  的精读、批量解读整个文库，以及课题级的七项分析，现在统一提交到一个任务
  队列，而不是点哪里就在哪里跑起来。
- 同一篇论文上的同一项工作不会再重叠运行——第二次会被拒绝，而不是真的启动，
  这就是为什么一篇论文的结果不会串到另一篇上。不同论文之间仍然可以同时跑。
- 同时运行的模型调用数量有上限，超出的排队等待，按提交顺序，前面的跑完就
  轮到后面的。

### 新增「任务」面板
- 新面板——工具栏按钮打开，像其它面板一样可以停靠，有任务在跑时按钮上会
  显示数量——列出正在运行、排队等待和已完成的任务，带进度条、已经跑了多久，
  以及大概还要多久。
- 任务可以单个取消，也可以一次全部取消；失败的可以重试，完成的可以清除。
- 刚提交、还在排队的任务，会在发起它的那个面板里标出「排队中」，点了不会
  像没反应一样。

### 关闭窗口不再悄悄丢掉进行中的工作
- 有工作还没跑完时关闭窗口，现在会先问一句，并列出还没完成的是什么。
- 执意关闭的话，没完成的工作会被记下来；下次启动时会问要不要接着跑，还是
  直接扔掉。没法诚实地接着跑的工作，不会被拿出来问。
- 库级分析如果什么结果都没跑出来，现在会报告失败，而不是悄悄卡住队列。

## v1.2.19 — 2026-08-25

### 文库分析改成面板
- 分类、研究地图、共识与冲突、时间线、覆盖度、机会点、下一步，现在停靠在
  论文旁边，而不是弹窗盖住论文——看到一个分类，可以同时打开它提到的那篇。
  和其他面板一样可以拖到布局的任意位置，工具栏按钮用来显示/隐藏。
- 底部新增**全部生成**：七个标签页排队依次生成，运行中点它即可停止。每个
  标签页仍保留自己的生成按钮，用于单独重写某一项。

### 目录名换成品牌名
- 设置、文库数据库和各类缓存原本放在 `ai-reader/AI Reader` 下——前者是代码
  仓库名而不是品牌名，后者带空格，下游每个脚本都得加引号。现在统一为
  `D2S/AIReader`。
- 升级会把已有数据搬过去：文库、段落缓存、下载的 PDF 和全部设置在首次启动
  时迁移一次，系统钥匙串里的 API Key 不受影响。第二次启动不会重复迁移，
  迁移之后新产生的数据也不会被覆盖。

## v1.2.18 — 2026-08-25

### 工具栏能看清了
- 一排二十五个带文字的按钮已经不能叫工具栏了。现在换成图标,并按作用对象分组——
  文件、视图、这篇论文、面板、课题、程序——组与组之间有分隔线,文字移到了提示里。
- 图标是同一套手绘线条风格,并跟随主题着色,明暗两种主题下看起来一致。
- 带数字的按钮保留数字:缩放比例、批量解读的进度、对比篮子里的篇数、翻译失败的段数。

### 重新分段会先问一句
- 对已经分过段的论文点「分段」,现在会告诉你它现在有多少段、其中多少段已翻译,
  以及重新分段会替换掉现有划分——译文是跟旧段落绑定的,重分之后大多对不上。
  第一次分段仍然直接执行。

## v1.2.17 — 2026-08-25

### 论文会停在你上次读到的地方
- 每篇论文的阅读位置会被记住,再切回来时回到原处——包括从课题里打开的论文
  (它在磁盘上的文件名是一串校验和),因为位置是按论文记的,不是按文件。这个位置
  只留在本机:笔记本屏幕上读到哪一行,和外接显示器上并不是同一个位置。

### Home / End / PageUp / PageDown
- 现在可以翻 PDF、段落面板、解读面板和各个对话框,**空格 / Shift+空格**也按阅读
  习惯前后翻页。正在文本框里打字时,这些键仍然归编辑用。
- 点一下某个面板,键就归它。

## v1.2.16 — 2026-08-25

### 切换论文重新变快了
- 打开另一篇论文时会去课题里取这篇的解读,而那次查找是把课题里**所有**解读都翻
  一遍——全部解析一次,包括每篇几十 KB 的精读。200 篇的库里,每切一次就是 19 毫秒,
  而且随着库变大越来越慢。
- 现在自己的解读是直接定位的(它的身份由课题、论文和你三者推导出来,根本不用找),
  协作者的则走一个只在数据变化时才重建的索引。同样 200 篇:0.8 毫秒。

## v1.2.15 — 2026-08-25

### 窗口标题显示论文名,不再是一串哈希
- 从课题里打开的论文是从共享缓存播放的,那里的文件名是校验和。标签页早就会去
  文献库里查真正的标题,但窗口标题栏还在直接读磁盘上的文件名,于是显示成一串
  哈希。现在两者问的是同一个地方。

## v1.2.14 — 2026-08-25

### 对比篮子跟着账号走
- 你挑出来准备对比的论文,原来存在这台机器的配置文件里,换台机器就没了。现在和
  解读层产生的其它内容一样存进课题——已有的篮子会在第一次运行这一版时自动迁移。
- 其余的本来就已经同步:解读(你的和协作者的,各自署名)、库级分析、研究档案、
  你的笔记,以及「待精读 / 搁置」标记——后者记在论文本身上,所以整个课题都看得到。

## v1.2.13 — 2026-08-24

### 老的「摘要」面板删掉了
- 结构化的**解读**面板已经完全取代它——回答的是同一批问题,而且每句话都能回到
  原文——所以旧的自由格式摘要、它的提示词页签和「共享」窗口一并删除,不再留一个
  更差的第二份。课题里的数据不会被删,只是旧的共享摘要不再有单独的窗口;取而代之
  的是带署名共享的解读。

### 工具栏
- **新增「段落」按钮**,和其它面板一样可以显示/隐藏段落面板,并且会记住。
- 状态栏现在显示**当前在第几页**,而不只是一共多少页。

### 设置
- **「解读」和「对话」合并成一页**「解读与对话」——两页加起来一共才三个设置项。

## v1.2.12 — 2026-08-24

### 换模型立刻生效,不用重启
- 以前切换服务商之后,程序还会继续用上一个,直到重启才对。原因是每个功能都把
  自己的连接缓存了,下次请求时只改字段——可切换服务商需要的是另一种连接
  (Anthropic 和 OpenAI 的协议不一样),而且它重新套上去的地址是配置里存的那个,
  不是该服务商自己的地址。
- 现在只有一个地方决定「连接什么时候必须重建」:服务商、模型、地址、密钥任一
  变化都重建;但正有请求在飞的时候不会重建——那会让那个请求永远等不到回音。

### 批量解读在窗口之外也看得见
- **关掉「解读全库」窗口从来都不会停止解读**——只有「停止」会。现在工具栏按钮
  会直接显示 *正在解读 12/57*,窗口里也写明了这一点。

## v1.2.11 — 2026-08-24

### 远程桌面下可用了
- **程序现在会认出自己跑在远程会话里**(Windows),改用软件绘制而不是走显卡。
  RDP 会话里根本没有显卡可用:常规路径会把每一帧渲染成一整张窗口图片再编码传输,
  正好撞在这个协议最不擅长的地方。软件绘制只重画变化的那一小块。
- 这种模式下**悬停和开合的小动画会关掉**。120 毫秒的颜色渐变在本地不算什么,
  在远程桌面上却是一连串整窗帧。
- 设置 → 界面 → **远程桌面**可以强制开/关,下次启动生效,并会显示当前这次会话
  是不是在用软件绘制。

### 拖动分隔条
- **解读面板现在会在拖动期间保持布局不变**,和段落、摘要、对话面板早就在做的
  一样——每次鼠标移动都把整篇解读重新排版才是真正贵的地方。在段落面板上实测:
  100 步拖动,实时排版 294 毫秒,保持布局 2 毫秒。
- PDF 视图不再在拖动的每一个像素上重排页面表格,松手时结算一次。

## v1.2.10 — 2026-08-24

### 服务地址跟着服务商走
- **Anthropic、OpenAI、DeepSeek 各自只有一个地址**,不再需要你来填:选了服务商,
  程序就去它那里。Base URL 输入框只在选 **openai-compatible** 时才出现——那一项
  本来就是为自建网关准备的。
- 这是个真 bug,不只是碍眼。切换服务商之后,上一个服务商留下的 Base URL 还在被
  使用,于是每个请求都发到了错的服务器上,带着那台服务器根本不认识的模型名——
  这就是为什么不管改什么配置,都只会得到一个没头没脑的 *400*。
- **选了 openai-compatible 却没填地址,现在算「未配置」**,而不是悄悄退回 OpenAI
  的官方地址。
- 「获取模型列表」也走同一个地址解析,和真正发请求的地方一致。

## v1.2.9 — 2026-08-24

### 模型拒绝的时候,现在会说清楚为什么
- 以前请求失败只显示*「Error transferring … — server replied with status code
  400」*,既没说原因也没说怎么办。服务商自己给的解释被丢掉了:流式请求的错误
  响应体走的是和正文同一个通道,等到请求结束时已经没有东西可读了。
- 现在会带上服务器真正说的话——比如*「HTTP 400: This model's maximum context
  length is 65536 tokens」*——OpenAI 系和 Anthropic 系的客户端都一样。
- 而且只要有明摆着的解法就直接写出来:上下文超了指向*设置 → 模型 → 上下文窗口*,
  输出长度被拒指向*设置 → 解读 → 最大输出 token*。
- 失败时还会把请求的形状(模型、输出上限、提示词长度、第几次尝试)写进
  launch.log,这样别人报过来的问题不用复现就能定位。

## v1.2.8 — 2026-08-24

### 设置改成按主题分类,不再是一长条
- **左边分类,右边是该分类的内容**:模型、翻译、解读、对话、界面、文档、更新与隐私。
- 本来就和模型无关的东西从「模型」页搬走了。字号、界面语言、对话输入、分段、共享、
  更新设置各归各位;模型页里只放模型。
- **翻译模型也有「获取」按钮了**,而且有自己独立的模型列表——它可能指向完全不同的
  网关,两个列表不会再互相覆盖。

### 标签页跟其它地方一个语言
- 研究窗口、解读面板、提示词对话框的标签页现在是「分段控件」,和按钮同一套样式:
  选中的是实心,其余的安静待着,鼠标移上去才有反应。
- 精读标签页里的按钮不再紧贴着上面的标签栏。

### 零碎
- **编辑课题**:保存和关闭调换了位置,确认按钮的位置和其它对话框一致了。研究档案
  对话框同样处理。

## v1.2.7 — 2026-08-24

### 主模型负责读,只有翻译单独配
- **设置最上面那个模型现在负责「读」**——快速解读、精读、库级分析、对话、摘要
  和视觉解读都走它。
- **翻译是唯一可以指到别处的**。翻译要跑遍每篇论文的每一段,通常用便宜快速的
  模型更划算。设置 → *翻译模型*;哪一项留空就沿用主配置。

### 正在读的那一篇又会高亮了
- **文献库**和**文件夹**两个面板都会高亮当前打开的论文。从课题里打开的论文是
  从共享缓存里以 sha256 命名的文件播放的,所以只按文件路径比对时,恰恰是协作者
  加进来的那些论文不会高亮——现在文献库按论文本身比对,文件夹面板也知道它是从
  哪个文件来的。

### 所有对话框一个样子
- 应用里所有弹窗现在都是设置/提示词对话框那套外观:同样的面板、同样的遮罩、
  同样的标题栏、同样的底栏。
- **所有按钮都是同一个按钮**——就是那两个对话框上的「确定」(实心)和「取消」
  (描边),高度统一。输入框、下拉框、数字框也一并对齐。
- 这些现在是共享组件,不是把样式抄进每个对话框,以后新加的对话框直接继承这套
  外观。

## v1.2.6 — 2026-08-24

### 模型不支持工具调用时,解读也不会因此失败
- 解读是通过「调用一个工具」来拿结构化结果的,这是最稳的方式——但如果网关上的
  模型部署时没有配工具解析,它会直接拒绝这个请求。以前这会让整次解读就此结束。
  现在会改用普通文本再问一次,并从回答里把 JSON 抠出来,老一些或弱一些的接口
  照样能用。
- 修掉一个会一直卡住的问题:精读正在跑的时候改解读模型,底层连接会被销毁,
  那次运行既不会完成也不会报错,就那么挂着。

## v1.2.5 — 2026-08-24

### 解读部分全部中文化
- 984 条界面文案全部翻译完成,包括新增的解读面板、精读、批量、对比和研究视图。

### 补上设计里漏掉的三件小事
- **引用现在会写明章节,不只是页码**——提示和导出里都是「4.2 实验设置,第 6 页」。
- **重新生成解读可以撤回。** 上一版会保留下来,解读面板 → ⋯ → *恢复上一个版本*。
- **分类除了合并也能拆分**:勾出该单独成类的论文,它们会移进旁边一个新分类;
  新分类归你所有,重新生成分类体系时不会动它。

## v1.2.4 — 2026-08-24

### 一整个课题的论文加起来说明了什么
**工具栏 → 研究**:对已经解读过的论文打开七个视图。全部是拿这些解读写的,
不读 PDF,而且都在课题里共享。

- **归类**:按真正能切开这个库的维度归类——研究问题、方法路线、任务、数据、
  指标、贡献类型、主要局限、相关度——一篇论文属于几类就在几类里。这套体系是
  你的:改名、锁定、合并、自己新建、把某篇论文从某一类里拿出来。**锁定过和
  改过名的类别,重新生成时不会被推翻**;后来加进来的论文按你确认过的体系归类,
  而不是把体系整个重画一遍。
- **研究地图**:这个库在绕着哪些问题转、每个问题下有哪几条路线、哪些论文走哪
  一条、每条路线走到哪里为止。
- **共识与冲突**:哪些结论有多篇论文独立支持、哪些只靠一篇、哪些被反复提起却
  从没被独立验证过、哪些是真的互相矛盾——真冲突和只是实验条件不同造成的表面
  冲突分开说。
- **演进**:问题、方法、数据和评价标准是怎么变的,以及从头到尾一直没解决的
  问题。
- **覆盖**:这个库哪些主题充分、哪些稀少,哪些结论证据很薄,哪些方法从没被公平
  对比过、从没在真实环境里验证过。**「这说的是你的库,不是这个领域」**那句话
  钉在最上面,导出时也跟着走。
- **候选机会**:从局限和冲突里读出来的候选研究问题,每一个都带上最小的验证
  实验、拿什么做对照、风险,以及老实给出的难度和可信度。并且分清楚这是某篇
  论文自己留下的问题、本库的覆盖空白,还是需要外部查新才能确认的空白。
- **行动建议**:接下来精读什么、搜什么、复现什么、对比什么、先做哪个小实验、
  该问导师什么。

这些视图里出现的每一篇论文,点一下就能打开。每个视图都保留最近几版,所以谁重新
生成一次都不会让别人的东西悄悄消失;整体可以导出成一份 Markdown 报告。

## v1.2.3 — 2026-08-24

### 解读可以导出成 Markdown
- **导出单篇**(解读面板 -> ... -> 导出为 Markdown):把快速解读、精读和你自己的
  笔记写成一个文件,而且出处照样带着。作者说的仍然写着是作者说的,模型想说但
  拿不出证据的仍然标着*没有找到证据*,核对不上的引用会作为「未核实」导出,
  不会被悄悄删掉。
- **导出比较**:真正的 Markdown 表格,可比性提示写在表格前面。
- 也可以导出整个课题的报告,而且带着那句要紧的话:这个库没覆盖到的,不等于
  这个领域不存在这样的工作。

### 文献列表上能看出哪些读过了
- 每篇论文标题前面一个小圆点:已解读(判断为高相关时是绿色),或者红色——
  解读失败,或者 PDF 里没有足够的文字。鼠标停上去会说明是哪一种。

## v1.2.2 — 2026-08-24

### 精读一篇论文,一部分一部分地读
- **解读面板多了一个页签:精读。** 九个部分——这是篇什么论文、读懂它需要的背景
  和术语、方法一步一步是怎么走的、实验、贡献、批判性阅读、局限、能不能复现、
  接下来该做什么。
- **每一部分是单独写的**,所以哪一部分写得不好,一次请求就能只重写那一部分,
  另外八部分不动;中途停掉,已经写完的部分也还在。
- 批判性阅读是按维度逐条给结论,不是笼统夸两句;复现那部分在论文没写的地方
  就写「说不清」,而不是编一个代码仓库链接。

### 每一条结论都可以追问
- **任意一条结论后面的 ⋯**:讲得更简单些、举个例子、把背后的公式或图讲一遍、
  质疑它,或者就这条继续问——每一个都会打开对话并把问题写好,而且是针对你正在
  读的这篇论文。
- **存为笔记**会放进「笔记」页签。笔记是你的:重新生成解读永远不会动它。
- **加入对比**会把这篇论文放进一个篮子里,关掉软件也还在。

### 跨论文比较
- **工具栏 → 对比**:把你收集的论文按十一个维度并排放——研究问题、假设、方法、
  输入输出、数据、基线、指标、主要结果、贡献、局限、复现条件。
- **提示写在表格前面。** 数据不同、指标不同、任务不同的论文,不能按数字排名,
  这里会直接说出来,而不是让一张整齐的表格暗示谁赢了。解读里没说的格子写
  「未说明」,不猜。
- 比较是拿每篇论文自己的解读做的,不是拿 PDF,所以十几篇论文比一次只要一个请求。

## v1.2.1 — 2026-08-24

### 一次解读整个课题库
- **工具栏 → 解读全库**:把课题里的每篇论文都读一遍,而且一篇都不用打开。
  本机从没见过的 PDF 会先从课题里取回来,后台分好段再解读——全程不动你正在
  读的那一篇。
- **有进度,失败也能处理。** 每篇显示排队中 / 处理中 / 已解读 / 失败,失败的
  原因就写在那一行上,「重试失败的 N 篇」只重跑这几篇。
- **然后就可以筛。** 全库解读完之后,按相关度、按阅读建议筛选,再对筛出来的
  结果批量操作:全部标记为待精读,或者全部搁置。
- **不会重复解读。** 已经有解读的论文——你自己的或协作者的——直接跳过,一个
  课题里每篇论文只付一次钱。
- 批量时分好的段落会保留下来,以后打开这些论文是秒开。

## v1.2.0 — 2026-08-24

### 论文现在可以被「解读」,不只是被「总结」
- **新增「解读」面板**,读完当前论文后直接回答你真正关心的问题:一句话是什么、
  跟**你这个课题**有多相关、先读哪一部分、它主张了什么、验证出了什么、弱在哪里。
- **每一条结论都标明出处**——是作者说的、是这篇论文的实验证明的,还是模型自己的
  分析。不会有哪句话不明不白地借用作者的权威。
- **后面那个小小的页码标签是核对过的引用。** 模型必须引用它依据的那一段原文,
  程序会回到那一段重新核对一遍才显示。点一下,PDF 和段落列表一起跳过去。
  引文在原文里找不到的,标灰显示为「未核实」,而依赖它的那句结论会被降级成
  「AI 分析」——一句听着笃定、背后却什么都没有的话,不会被当成结论。

### 课题可以说明自己是干什么的
- **工具栏 → 档案**:填写这个课题的研究目标、核心问题、范围与不考虑的范围、
  你当前所处的阶段。所有解读都以它为准来写,所以「相关度」和「阅读建议」说的是
  你的工作,而不是泛泛而谈这个领域。
- **改了档案,已有的解读会提示可能已过期**——重新分段、换模型也一样。

### 解读是共享的,不用重复付费
- 解读和其它内容一样存进课题,并标明是谁跑的。协作者读过的论文,解读会直接出现在
  你这边,不用再花一遍 token。

### 解读可以用和其它功能不同的模型
- **设置 → 解读模型。** 批判性地读一篇论文,对模型的要求比翻译一个段落高得多。
  把它指向更强的模型——甚至另一个服务地址——翻译仍然走原来的。留空的项自动沿用
  主配置。

### 对话输入
- **输入框变高了**(高度也可以在设置里调)。
- **默认回车发送**,Shift+回车换行。想要原来的行为,设置 → 对话输入里改回
  Ctrl+回车发送。

## v1.1.24 — 2026-08-23

同时捎带上了 v1.1.20 到 v1.1.23——这几版都没有发布,所以从 1.1.19
升上来的话全都在里面。

### 段落面板标题显示已翻译的段数
- **标题现在写成「段落(382 · 已翻译 300)」。** 一篇文章翻到什么程度,以前
  只有正在翻译时才看得见;现在只要翻过就一直显示。

## v1.1.23 — 2026-08-23

### 翻译进度不会再超出总数
- **修好了「正在翻译 419/382」。** 进度原本是一个累加计数器,而有两个地方
  往里加:每翻完一段加一,从缓存恢复一篇文章时又把它抬到「已翻译的段落数」。
  对一篇旧译文还在缓存里的文章点「从头再来」,翻译途中切走再切回来,同一段
  就被数了两次。现在这两个数字直接从段落本身数出来,不可能和屏幕上看到的
  对不上。

## v1.1.22 — 2026-08-23



### 点翻译时会问你想要哪种
- **对翻译了一半的文章点「翻译」,现在会让你选**:只翻还没翻的段落,还是
  从头把每一段都重新问一遍模型。以前按钮默默做的是前者,而后者除了一段段
  右键之外根本没法做。一段都没翻过的文章不会弹窗,直接开始。

### 一眼看出正在读哪篇
- **库里会高亮当前打开的那篇**,跟文件夹树一样。两个面板现在用同一套判断
  (比较规范化后的路径),所以目录是软链接、或者路径写法不同,也照样能高亮。
- **从库里打开的文章显示标题,不再是一串哈希。** 这类文件是从按内容寻址的
  缓存里取的,磁盘上的文件名就是 `<sha256>.pdf`——标签页和「精读」面板显示的
  正是它。现在显示库里记的标题,库里不认识的文件仍然显示文件名。

## v1.1.21 — 2026-08-23

### 两篇可以同时翻,几条线由你定
- **开第二篇不用再等第一篇。** 1.1.20 已经让翻译任务按文章归属,但队列还是
  一条,第一篇的每一段都排在第二篇的第一段前面。现在在飞的请求按文章轮流分配。
- **同时翻几段可以设置了**——设置 → 模型与语言 →「同时翻译段落数」,
  1 到 16,默认仍是 2。这是所有正在翻译的文章**共用**的一个额度,不是每篇
  各自的;改了对正在跑的任务立即生效。

### 设置
- 第一块设置(服务商、模型、密钥、生成参数、语言)一直没有标题,和下面每一块
  都不一样。现在补上了「模型与语言」。

## v1.1.20 — 2026-08-23

### 取消能真的取消,切换文章不再打断翻译
- **点取消真的会停。** 以前它只清空队列,已经发出去的请求「让它们自然结束」
  ——同时有两个在跑的时候,这和按钮坏了没区别:段落继续往外冒,按钮一直显示
  「取消」,模型那边照样计费。现在会直接中断。翻到一半的段落会清掉,而不是
  挂着「已翻译」的角标留半句话;取消也不算失败。
- **翻译任务属于它那篇文章。** 以前切标签页就把它打断了。现在各翻各的:
  让一篇在那儿翻,你去读另一篇,回来它已经翻完了。你不在的时候出的结果会写进
  那篇自己的缓存,回去就能看到。
- **进度只统计你正在看的这篇**,同时会写明还有几篇在后台翻,免得后台的活
  完全看不见。关掉某篇的标签页就会停掉它——否则一篇谁都没打开的文章还在
  烧 token。
- **改段落仍然会取消那篇的翻译**,因为拆分和合并会把排队任务依据的段落编号
  全打乱。现在判断依据是段落本身真的变了,而不是「段落列表发了个信号」——
  后者正是普通切标签被误判成编辑的原因。

## v1.1.19 — 2026-08-22

### 分段和翻译现在属于课题
把一篇论文分段要花几秒 CPU,翻译要花模型 token。以前这两样都只躺在本地的
JSON 缓存里,换台机器、换个人就得重新付一遍。现在它们是课题里的对象,和
题录、附件一样同步。

- **自己的另一台机器直接拿回来。** 在台式机上分好段、翻译好的论文,笔记本
  打开就有,不用再跑一遍分段,也不用再花一次 token。键是文件内容的哈希,
  所以 PDF 改了名、换了目录也照样认得。
- **没做过这份活的协作者直接用你的。** 课题里别人已经分过段的论文,你打开
  就有段落。
- **拉来的东西都标明出处。** 段落还是别人的时候,「段落」标题栏上挂一个
  「分段来自 name@example.com」的标签;译文是从课题里取来的段落,每一段
  自己带一个「来自 name@example.com」的角标——译文是一段一段采纳的,
  同一页可能你自己翻的和同事翻的混在一起。你自己重做那份活,标签就消失。
- **自己做过的永远优先。** 段落列表是整份的——你一旦分过段或改过段落,
  别人的分段就不会覆盖它。翻译是按段落算的:你自己翻过的那段永远不会被
  盖掉,别人的译文只会补上你还没翻的段落。拉下来的成果会被标记,不会再以
  你的名义传一遍,免得五个人的课题里存了五份一模一样的东西。
- **一个开关就能关掉。** 设置 → 更新与隐私 → 「共享到课题」。关掉之后,
  论文正文一个字都不会离开这台机器,本地缓存照旧。
- 上传前会先压缩——790 KB 的分段结果传上去约 200 KB;压缩后仍超过 4 MB
  (或服务端上限一半)的论文会跳过共享,而不是把同步队列卡住。
- **服务端接不住时一个字都不传。** 服务端现在会在每次拉取时告知它能接受
  多大的请求体;遇到不说这句话的旧服务端,分段和译文就留在本地。否则一次
  被拒的推送会把发件箱堵死,连普通的题录改动都同步不出去。

### 同步扛得住这些大对象了
- **拉取分页了。** 以前首次同步一个用得比较久的课题会一次性返回全部内容;
  现在按每页 200 个对象走,游标停在已经落库的位置上,中途断了可以接着拉,
  不用从头再来。
- **推送按条数和体积分批**,单个请求不会太大,发件箱稳步清空。
- **服务端收得下了。** 之前 JSON 请求体上限还是 Express 默认的 100 KB,
  比一篇分好段的论文还小。
- **论文数据走索引,不做全表扫描。** 打开一篇论文是按键查它的分段,而不是
  把课题里所有同步下来的大块数据都解析一遍。

## v1.1.18 — 2026-08-21

### 选中即译,就在选中的地方
- **在 PDF 里选中文字,右键选「翻译选中内容」。** 译文卡片就贴着这段文字
  弹出来,模型边写边显示,卡片上的「复制」可以把译文带走。
- **卡片是钉住的。** 失去焦点、重新选别的文字、滚动页面,它都不会消失,
  只有点 × 才关。按住卡片顶部可以拖着走;想开几张就开几张,新开的一张
  压在最上面,并且会自动错开已有的卡片。
- **如果这段选中的文字落在软件认得的某个段落里,翻的就是那整段。** 用的
  是右侧面板的同一行、同一份本地缓存,所以右侧会一起填上;已经翻过的段落
  再选中时直接出结果,不会再花一次接口调用。
- **其他情况就单独翻你选中的那几句** ——比如跨了两段、只选了一个短语,
  或者这篇还没分段。这类结果也会按论文缓存下来。

### 拖动面板分隔条不再卡
- **分隔条现在有像样的抓取区和光标。** 之前是 4 像素宽、鼠标放上去还是
  普通箭头,既不好抓,也没有任何提示说它能拖。现在抓取区 10 像素(可见的
  线仍是 4 像素),悬停时变成左右拉伸光标。
- **拖动时面板照常实时重排,只是不再每次鼠标移动都排一遍。** 此前每移动
  一下,段落面板的每一段、聊天的每条气泡、解读面板的整篇文档都要重排:
  实测一次移动 3.1 ms、3.1 ms、7.8 ms——一帧的预算就这么被吃光了。现在
  拖动期间按每秒约 30 次重排,松手时对齐到精确宽度。改完后同样的测量:
  0.10、0.16、0.71 ms。
- **段落列表重排不再把 PDF 顶走。** 行高变化会挪动列表的滚动位置,而这被
  当成"用户滚到了第 N 页"上报,于是拖动分隔条会顺带让 PDF 跳页。拖动期间
  不再上报位置。
- PDF 面板实测 0.07 ms/像素,本来就不是瓶颈,没有改动。
- 面板宽度改为松手后 300 ms 写一次,而不是整个拖动过程里每 300 ms 写一次
  ——代码注释本来承诺的就是前者。

## v1.1.17 — 2026-08-20

### 适应宽度是真的刚好
- **单击缩放比例,现在正好放大到不出现横向滚动条为止。** 之前是拿面板
  的外框宽度去除,而页面所在的列比外框还要窄两个像素,于是每次都刚好
  越过溢出的那条线,横向滚动条必然冒出来。现在会放到不出条的最大档。
- **旋转过的文档按旋转后的宽度来适应。** 适应宽度一直按正立的页面算,
  视图转了 90° 时就会差出一个页面长宽比。
- **不足一个像素的溢出不再算溢出**,横条不会因为一点舍入余数又闪回来。

### 模型卡住时,不再看起来像软件卡住
- **请求没动静就会超时。** 服务端接了请求、回了 200、然后一个 token 都
  不发时,这个请求原来会一直挂着:没有报错,没有进度,而翻译是两个段落
  并发的,两个位置被占死,整批翻译就停在那里,界面上还没有任何提示。
  现在完全没有数据的状态持续两分钟就结束请求并报错。这个计时在每收到
  一个字节时重置,所以只要模型还在慢慢吐字,就不会被切断。
- **流式响应里报的错会显示出来。** 网关必须在知道模型会不会作答之前就
  先回 "200 OK",所以只能把失败当成流里的一个事件发过来。这类事件原来
  被直接丢掉,请求就只是空着结束了。
- **翻译结果为空算失败,不算结果。** 空白的段落原来会被标成"已翻译",
  而且这个空白还会写进本地缓存,于是重新打开论文时"恢复"出来的还是
  空白,这一段永远不会被重试。现在它会被标成失败、不写缓存,并且能被
  "重试失败项"重新捞起来。

## v1.1.16 — 2026-08-20

### 滚动条只在真有内容可滚时出现
- **PDF 区域不再是任何缩放下都挂着横向滚动条。** 页面所在的列被排得
  比可视区还宽出一根滚动条,于是哪怕页面只占窗口一小块,内容在技术上
  也算"溢出"。现在只有页面确实比面板宽时才溢出——缩小,横条就消失。
- **单页论文缩小显示时,竖向滚动条也不再出现**,同样的原因:两根滚动条
  现在各自如实反映自己那个方向。
- **缩放后立即重排。** 此前重排发生在新页宽算出来之前,可滚动宽度会
  滞后一档缩放,直到别的操作触发重排才跟上。

### 文件夹面板可以按文件夹勾选
- **文件夹那一行也有勾选框了。** 勾上就选中它下面的所有 PDF——含子
  文件夹,也含你没展开过的部分;只选中了一部分时显示半选状态。原来的
  右键菜单仍然保留。

### 移动面板
- **面板拖柄改用移动光标**(四向箭头),而且整个拖动过程都保持这个形状,
  不会被路过的其它面板抢走。拖动被弹窗或切换窗口打断时,插入标记也会
  正确清除。

## v1.1.15 — 2026-08-20

### 分段改成你点了才做
- **打开论文不再自动分段。** 长论文分段要占好几秒 CPU,还要跑一趟
  GROBID,而很多时候你只是想翻翻页面。需要段落时,点工具栏的
  **分段**(段落面板空着时那里也有一个按钮),走的是同一条流水线。
- **新增设置**(段落分段一节):"首次打开论文时自动分段",默认关闭。
  已经分过段的论文仍然从缓存里秒开。

### 一次把整个文件夹加进文献库
- **在文件夹树里勾选 PDF,一次性批量加入。** 树上方的工具条把勾选的
  文件加入当前课题:逐个算哈希、建立文献条目、按 DOI/arXiv 补全信息
  并上传,全程不用打开任何一篇。课题里已有的论文会跳过,不会重复。
- **右键点文件夹**可以选中(或直接加入)它下面的所有 PDF,含子文件夹
  ——树里只列出你展开过的部分,所以这一步是直接读文件夹的。

### 选择与标签页
- **Ctrl+C 可以复制选中的 PDF 文字了**(macOS 上 ⌘C 和物理 Control 键
  都行)。此前拖选文字不会让 PDF 区域进入键盘焦点链,快捷键因此落空,
  只有右键菜单能复制。
- **右键点标签页**:关闭此页 / 关闭其他页 / 全部关闭。

## v1.1.14 — 2026-08-20

### 文献库会告诉你它在干什么
- **上传、下载和检查的进度现在会显示**在文献库面板底部。此前这些
  状态只在内部记录、界面上无处可见,导致上传失败和成功看起来
  一模一样。

### PDF 确实存进云端后才记账
- **只有字节真正到达云端,才会把论文标记为"已有 PDF"。** 此前是
  先写记录再传字节,一旦上传失败就会留下"这篇有 PDF"的假记录,
  别的设备照着它去下载,必然失败。
- **新增「检查 PDF」按钮**,把课题与云端实际内容对账:PDF 缺失
  但原文件还在本机的,自动补传;文件哪儿都找不到的,退掉那条
  记录,不再让其它设备白跑一趟。文献条目本身不受影响。

- 课题设置对话框里的按钮统一了大小。

## v1.1.13 — 2026-08-20

- **从文献库打开的论文不再是空白页。** 文献库交给阅读器的是裸文件
  路径,而这里需要的是 file:// URL,于是 PDF 区域一直空着、段落列表
  却有内容;同样的原因让论文被当成远程文件,跳过了 GROBID,段落
  退化成大量碎片。现在所有打开论文的入口都会统一归一化路径,任何
  调用方都不会再触发这个问题。

## v1.1.12 — 2026-08-20

### 文库 PDF 在外网也能同步了
- **PDF 的上传下载改为经由 API 服务器中转**,不再让客户端直连
  对象存储。此前服务端签发的临时链接指向内网地址,导致「加入
  文库」和「在另一台设备上打开同事的论文」只在公司网络内可用,
  在外网静默失败。现在字节流和其它请求走同一个域名、同一套
  登录态,对象存储不再对客户端暴露。上传仍按内容哈希去重,
  别人已经加过的论文不会重复上传。

## v1.1.11 — 2026-08-20

### 课题可以改名和删除了
- **课题选择框旁新增「编辑课题」按钮**:重命名课题或修改描述
  (需要编辑或所有者权限);所有者还可以删除课题。删除前需要
  手动输入课题名称确认,并会告知有多少本地修改尚未同步 ——
  因为删除会为**所有成员**永久移除其中的文献、笔记、批注和 AI
  结果。删除同时清理该课题的本地数据、同步游标和搜索索引,
  以前这些会作为无法访问的孤儿数据永久残留。

### PDF 滚动条回来了
- **PDF 区域现在有真正的滚动条**,有内容可滚时常驻显示,可以
  直接拖动。滚动条其实一直存在,只是实际上永远看不见:应用是
  直接移动视图来平移的,从不触发自动隐藏滚动条的淡入。

### 目录会说明自己的来源
- 目录现在标注**「来自论文结构」**或**「AI 生成」**,按钮也从
  「刷新」改为**「用 AI 重建」** —— 它会丢弃结构化目录并消耗一次
  模型请求,旧措辞掩盖了这个代价。

## v1.1.10 — 2026-08-20

针对 v1.1.9 实测反馈的五个问题的修复版。

### 重新分段真正可用
- **点「重新分段」不再没反应(也不再真的没反应)。** 修复三个叠加
  问题:提取的几十秒里毫无反馈(现在按钮置灰、状态栏显示
  「分段中…」、段落面板转菊花);上一轮迟到的 GROBID 响应可能把
  新结果整个吞掉;翻译过的论文会被"保护翻译"守卫永远拒绝 GROBID
  升级。显式重新分段现在始终生效,已有无头回归测试覆盖。

### 自动更新重启,二度修复
- **应用启动安装器后立即自退**(Inno 官方自更新模式),不再等着
  被安装器在换文件时强杀 —— 强杀竞态可能让重启步骤静默失败。
  安装器同时把日志写到 AppData\update-install.log,以后再出问题
  可诊断。(从本版起的更新生效;1.1.9 → 1.1.10 这一跳可能仍需
  手动打开一次。)

### 标准按钮终于说中文
- **Windows 安装包现在携带 Qt 官方词条目录**(确定/取消/关闭等)。
  部署脚本一直传着 --no-translations,1.1.5 修的加载逻辑在用户
  机器上根本无米下锅。

### 服务端(无需更新)
- **同步 WebSocket 重连风暴已消除**:服务器上的产品主页规则把
  域名根路径的 WebSocket 握手也劫持了,所有客户端每 5 秒重试、
  永不停止(同步一直静默靠轮询兜底)。实时同步已对所有版本恢复。

## v1.1.9 — 2026-08-20

- **分段与目录合并为一个操作**:每次 GROBID 分段生效(首次打开或
  手动重新分段)都会同时按论文结构刷新目录 —— 旧目录引用的段落
  在新分段后已不存在。你手动发起的 LLM 生成仍然优先;规则就是
  「最新结果优先」,界面与缓存一致。
- **自动更新完成后自动打开新版本**(Windows):安装器的启动步骤
  在静默模式下被跳过,导致一键更新装完"没反应"。现在静默安装
  结束时显式重启应用(以普通权限)。

## v1.1.8 — 2026-08-19

### 目录直接来自论文结构
- **目录改由 GROBID 结构分析生成** —— 做段落切分的同一个服务顺带
  给出章节层级(编号感知的层级、每节所在页码),打开论文即自动出现
  完整目录,无需调用大模型。「生成」按钮仍走 LLM 路径,且 LLM 生成
  的目录始终优先于结构化目录。

### GROBID 服务加固
- **公共切分服务启用滚动验证码**(RFC 6238 一次性口令),应用自动
  携带,匿名扫描请求一律 401。旧版本将回落到内置切分器,升级后恢复。
- **设置里清空 GROBID URL 时回落到默认公共端点**(与更新清单 URL
  行为一致)—— 开关只由复选框控制。

### 修复
- 向导第 6 步的聚光灯不再在 CAS 会话恢复后漂到屏幕中央 —— 现在
  实时跟随「登录 / 账号」两个按钮中可见的那个。

## v1.1.7 — 2026-08-19

### 一键更新
- **"立即更新"自动下载并安装** —— 按钮内显示进度,静默安装,应用
  自动重启,不再跳浏览器手动操作(Windows;其它平台仍打开下载页)。

### 全局梳理
- **配色全面审计修复**:深色横幅上的不可读按钮、深色模式聊天气泡
  文字、段落状态徽标、分割条、拖拽柄、代码块控件等,深浅两套主题
  逐一验证。
- **中英文全面审计**:文件对话框过滤器、成员角色、题录类型、段落
  类型/状态等原样显示的英文代码全部改为翻译显示;词条目录 100%
  完成。

## v1.1.6 — 2026-08-19

- **更新检查在点击处直接给出结果**:设置中的结果行现在带下载按钮
  —— 之前窗口底部的横幅被模态对话框遮住,检查成功看起来像没反应。
- **失效更新地址自动迁移**:老安装里保存的已弃用 raw.githubusercontent
  清单地址(多数用户环境下静默不可达)会在启动时重置为服务器默认值。

## v1.1.5 — 2026-08-19

- **打包版中标准按钮跟随系统语言。** 确定/取消/关闭 等来自 Qt 自带
  翻译,应用此前只在构建机的 Qt 安装路径找它们;现在还会查找可执行
  文件旁的 translations 目录(及 mac 包的 Resources),中文系统显示
  中文按钮。
- 设置中更新清单地址不再提示已弃用的 GitHub URL;线上更新源
  (aireader.d2ssoft.com/update/manifest)已提供 v1.1.4+ 和可用的
  下载通道。

## v1.1.4 — 2026-08-19

- **缩放数字按钮行为调整**:单击适应页宽,双击恢复 100%。

## v1.1.3 — 2026-08-18

### 新手引导
- **每个新版本首次启动都会重播一遍使用向导**,带你过一遍该版本的
  变化。
- **向导新增两步**:通过 CAS 登录以跨设备同步文库;把课题文库共享
  给同事(owner / editor / viewer 角色、共享 AI 解读)。

### 界面
- **主按钮在深浅两套主题下统一为主流的"蓝底白字"**,深色模式下
  原先的浅蓝底深色字配色移除。

### 打包(Windows)
- 安装包对缺失 MSVC 运行库的加固:缺 CRT DLL 时拒绝打包;内置官方
  vc_redist 静默安装兜底;Windows 10(1809)以下系统在安装前即被
  拒绝,而不是装完打不开。

## v1.1.2 — 2026-08-18

流畅版:对着一篇 30 页论文做了全面性能剖析。

### 性能
- **秒开,顺滑滚动。** 清除了 UI 路径上所有触碰 QtPdf 全局
  PDFium 锁的调用:布局所需的页尺寸一次性缓存;段落提取与选择
  结构改在限速的工作线程中、待首屏渲染完成后构建;点击定位从
  逐字符扫描改为二分查找。

### 画布拖拽
- **不再拖出两侧空白。** 页面窄于窗口时只能上下拖动;宽于窗口
  时页面边缘贴到窗口边缘即停。滚轮横向滚动遵循同样的边界。

### 修复
- 抓手光标改为真正的实心填充剪影,指根处不再透出页面文字。
- 新安装的 GROBID 服务默认指向公网
  `https://aireader.d2ssoft.com/grobid`。
- 产品主页上线 https://aireader.d2ssoft.com(Windows 安装包已
  镜像到自有服务器)。

## v1.1.1 — 2026-08-18

针对 v1.1.0 文本选择与段落功能第一轮用户试用反馈的修复。

### 选择与段落
- **选择在哪里都可用**:每一页都能选(不再只有第一页),也能
  跨页选;双击不再选中相邻行的词;三击选中的是阅读面板中显示
  的完整段落。
- **重写渲染行提取**,段落切分器与文本选择共用同一实现 ——
  PDFium 的换行信息会少报视觉行,斜体等带样式的词也不再把段落
  切碎。此前打开过的论文在重新提取前沿用已缓存的切分结果。

### 光标
- 竖条(I-beam)/ 链接 / 抓手光标现在都能正确显示;抓手光标
  重绘为实心白色填充。

### GROBID
- **GROBID 服务已部署并默认启用** —— 新安装自动使用
  `https://aireader.d2ssoft.com/grobid`;已有安装可在
  设置 → 段落切分 中填写该地址。

## v1.1.0 — 2026-08-18

阅读体验版。

### PDF 文本选择,重写
- **浏览器级选择体验**:拖选可跨页;双击选词、三击选段,按住继续
  拖动按词/段扩展;拖到窗口边缘自动滚动。
- **光标会说话**:文字上是竖条(I-beam),链接上是小手(单击跳转),
  其余是箭头。
- **右键菜单**(复制 / 全选本页)与 Ctrl/Cmd+C、Ctrl/Cmd+A 快捷键。
- **复制出干净的文本**:断字连字符自动合并、段落重新拼接,不再是
  一行一断的碎片。

### 更聪明的段落识别
- **GROBID 集成**(默认开启,需可达的服务):新打开的论文由 GROBID
  的文档模型给出真正的段落 —— 跨栏、跨页自动合并;服务不可达时
  静默回退到内置切分器。在 设置 → 段落切分 配置;自建服务:
  `docker run -d -p 8070:8070 grobid/grobid:0.9.1-crf`。

### 界面
- **所有对话框统一重设计**:更精致的外观、清晰的主/次按钮层级、
  设置页分组卡片;修复更新日志与欢迎向导的深色模式显示问题。
- 新的抓手工具图标。

## v1.0.0 — 2026-06-29

云端版。v0.2.0 让 AI Reader 能在单台机器上服务真实用户;v1.0.0 让
一个团队跨机器共享同一个文献库 —— AI Reader 从本地阅读器变成了
协作式文献工作台。

### 云端文献库
- **以课题组织共享库。** 按研究课题归类文献,每个课题是一个云端
  同步的文献库,拥有各自的成员。
- **一键加文献并自动补全题录。** 打开 PDF 点 **+ 添加**:题名 /
  作者 / 年份 / 出处 会按 DOI 或 arXiv 号自动补全,PDF 同时上传
  (按内容寻址,同一文件全课题只存一份)。
- **整库全文检索** —— 题名、摘要、作者、标签。
- **离线优先同步。** 断网也能浏览、编辑;联网后自动对账(WebSocket
  实时,轮询兜底),按字段后写胜。

### 协作
- **多人课题**,owner / editor / viewer 三种角色,按邮箱邀请成员。
- **共享 AI 解读。** 把 AI 摘要 / 解读共享到课题,同伴在 **共享**
  里即可看到。

### 账号与更新
- **CAS 单点登录。** 浏览器一键登录,应用内不保存密码。
- **自建自动更新。** 启动时检查服务端版本清单,有新版一键下载。

### 界面
- **文献栏并入主工具栏** —— 课题选择、成员、账号不再独占一行。
- 以上功能**全部中文化**。

## v0.2.0 — 2026-05-02

打磨与发布版。v0.1.0 是第一个能跑起来的版本;v0.2.0 是第一个能在
开发者机器之外的真实环境中正常运行的版本。

### 对话
- **每篇论文支持多会话对话。** 每篇论文在对话面板顶部维护自己的
  会话标签栏:**+** 新建,**×** 关闭,双击重命名。
  会话标题在累计三轮对话后从首条用户消息中自动生成。会话按论文
  保存到 `<AppData>/cache/chat/<paperId>.json`。
- **类型化消息渲染。** 已完成的回复以一组类型化 QML 项渲染
  (文本、代码、数学公式),不再统一塞在一个 `TextEdit` 中。
  代码块显示语言标签和复制按钮;数学公式(`$$ … $$`)通过
  MicroTeX 渲染,失败时回退为黄底原始 LaTeX。
- **段落级翻译** 加入段落右键菜单 —— 不必重跑全文翻译就能单独
  翻译一段。

### 段落
- **段落级原文/译文显示开关。** 每段单独切换,持久化到段落缓存。
- **设置 → 字号(像素)** 可分别设置「章节目录 / 论文解读 /
  段落 / 对话」面板的正文字号。各面板内的标题与标签会按比例
  放大,保持视觉层级。
- **目录与解读不再被段落编辑清空** —— 当你拆分、合并、删除单段
  时,清理与重载的逻辑只在真正切换论文时触发。

### 界面与新手引导
- **首次启动新手向导** —— 半透明遮罩配合工具栏按钮的高亮
  spotlight。任何时候都可以从工具栏的 **?** 重新打开。
- **面板布局与尺寸会被记住。** 拖动任意面板左上角的 **⋮⋮** 拖柄
  来重新排列;拆分条的位置和顺序都会持久化。
- **流式输出时自动滚动到底部。** 对话与解读面板都支持。向上滚
  动会暂停自动跟随,这样阅读旧内容时不会被强制下拉。

### 更新与隐私
- **应用内更新检查。** 启动时拉取 GitHub Releases 上的小型
  `manifest.json`,有新版会在窗口底部显示蓝色横幅,带「下载」
  按钮。可在「设置 → 更新与隐私」中关闭。
- **崩溃报告(可选加入)** 通过 Sentry-Native(构建时需配置
  `-DAIREADER_ENABLE_SENTRY=ON`)。默认关闭;不收集任何 PII。
- **应用版本号显示** 在设置对话框页脚和安装包的 PE 资源元数据
  中(右键 → 属性 → 详细信息)。

### 打包
- **Windows 安装包流水线。** Build → `windeploy.bat` → Inno
  Setup `AiReader.iss` → 可选 `sign-windows.ps1` →
  `publish-release.ps1`。bat 会自动通过 `build\CMakeCache.txt`
  定位 `windeployqt`;部署会附带 MSVC 运行时 DLL
  (vcruntime140.dll 等),没装 Visual C++ 可再发行包的用户也能
  正常启动。
- **持久化启动日志** 位于 `<AppData>\AI Reader\launch.log`,
  方便诊断 GUI 子系统下吞掉 stderr 的启动失败。
- **MicroTeX 字体路径可重定位** —— 数学公式现在能在已打包的安装
  中正常渲染,不只在 `build/` 下的开发版可用。

## v0.1.0 — 2026-04-26

完成 M1–M4 的首个里程碑构建:PDF 渲染、段落抽取与手工编辑、并排
翻译、自动生成目录、解读面板、视觉读图工具,以及带 cmark-gfm
Markdown 与 7 个论文相关工具的对话面板。
